spring:
  datasource:
    url: jdbc:postgresql://localhost:6432/testdb
    username: testpguser
    password: testpgpass
  test:
    database:
      replace: none
  jpa:
    properties:
      hibernate.generate_statistics: true